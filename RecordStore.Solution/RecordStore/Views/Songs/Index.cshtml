@* @{
  Layout = "_Layout";
}

@using RecordStore.Models;
@model List<RecordStore.Models.Song>;

<h1 class="title">Songs</h1>

@if (@Model.Count == 0)
{
  <h3>No songs have been added yet!</h3>
} 

@foreach (Song song in Model)
{
  <p class="list__link btn btn-outline-dark btn-block">@Html.ActionLink($"{song.Title}", "Details", new { id = song.SongId }) | Album: @song.Album.Name</p>
  
}
<p class="btn btn-dark btn-block">@Html.ActionLink("Add new song", "Create")</p> *@
